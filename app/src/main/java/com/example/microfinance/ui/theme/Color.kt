package com.example.microfinance.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand Core ──────────────────────────────────────────────────────────────
val BrandPrimary       = Color(0xFF4F5D95)   // Indigo-blue — trust & stability
val BrandPrimaryLight  = Color(0xFF7C89B8)   // Lighter variant for surfaces
val BrandPrimaryDark   = Color(0xFF3A4570)   // Pressed / dark variant

// ── Accent ──────────────────────────────────────────────────────────────────
val BrandAccent        = Color(0xFFA88B5E)   // Micro-finance gold — warmth & growth
val BrandAccentLight   = Color(0xFFD4B98A)   // Light gold for chips / tags

// ── Backgrounds ─────────────────────────────────────────────────────────────
val BackgroundLight    = Color(0xFFF7F7FB)   // Soft off-white
val SurfaceCard        = Color(0xFFFFFFFF)   // Pure white cards
val SurfaceElevated    = Color(0xFFF0F1F8)   // Slightly tinted elevated surface

// ── Text ────────────────────────────────────────────────────────────────────
val TextPrimary        = Color(0xFF1F2430)   // Near-black — main content
val TextSecondary      = Color(0xFF6B7280)   // Muted grey — subtitles / hints
val TextOnPrimary      = Color(0xFFFFFFFF)   // White text on primary buttons

// ── Status ──────────────────────────────────────────────────────────────────
val StatusPaid         = Color(0xFF2E7D32)   // Green — paid / success
val StatusPaidBg       = Color(0xFFE8F5E9)   // Light green background
val StatusPending      = Color(0xFFE65100)   // Amber-orange — pending / warning
val StatusPendingBg    = Color(0xFFFFF3E0)   // Light amber background
val StatusClosed       = Color(0xFF546E7A)   // Blue-grey — closed / inactive
val StatusClosedBg     = Color(0xFFECEFF1)   // Light blue-grey background

// ── Divider / Border ────────────────────────────────────────────────────────
val DividerColor       = Color(0xFFE5E7EB)
val BorderColor        = Color(0xFFD1D5DB)

// ── Splash gradient stops ────────────────────────────────────────────────────
val SplashGradientTop    = Color(0xFF3A4570)
val SplashGradientBottom = Color(0xFF4F5D95)
